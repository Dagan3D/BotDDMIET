# Telegram Support Bot (Бот технической поддержки)

Асинхронный Telegram-бот для организации многоканальной технической поддержки. Бот использует функционал **тем (топиков)** в супергруппах Telegram для маршрутизации и изоляции обращений пользователей к разным командам специалистов.

## 🚀 Основные возможности

- **Маршрутизация обращений:** Пользователь выбирает категорию вопроса, и его обращение автоматически направляется в нужную группу специалистов.
- **Изолированные диалоги:** Каждое новое обращение создает отдельный топик, что позволяет специалистам вести обсуждения, не смешивая разные заявки.
- **Двусторонняя связь:** Сообщения легко пересылаются между личным чатом пользователя и соответствующим топиком в группе.
- **Управление состоянием:** Используется FSM (Finite State Machine) из `aiogram` для ведения пользователя по процессу создания заявки.
- **Сохранение сессий:** Информация об активных диалогах хранится в базе данных SQLite для обеспечения стабильной работы.
- **Чистая архитектура:** Логика, конфигурация и утилиты для работы с БД разделены на разные модули.

## ⚙️ Как это работает

1.  Пользователь пишет боту `/start` и выбирает категорию вопроса из предложенных кнопок.
2.  Бот создает новый топик в закрытой Telegram-группе, которая соответствует выбранной категории.
3.  Сообщение пользователя с описанием проблемы пересылается в этот топик.
4.  Специалисты поддержки видят обращение и отвечают на него прямо в этом топике.
5.  Ответ специалиста пересылается ботом обратно пользователю в личные сообщения.
6.  Диалог продолжается до тех пор, пока специалист не закроет обращение командой `/close` в топике, или пользователь не напишет `/stop`.

## 🛠️ Стек технологий

- **Python 3.10+**
- **Aiogram 3.x** — асинхронный фреймворк для создания Telegram-ботов.
- **Aiosqlite** — для асинхронной работы с базой данных SQLite.
- **python-dotenv** — для управления переменными окружения.

## 📦 Установка и запуск

1.  **Клонируйте репозиторий:**
    ```bash
    git clone [URL вашего репозитория]
    cd [название-папки]
    ```

2.  **Создайте и активируйте виртуальное окружение:**
    ```bash
    python -m venv venv
    source venv/bin/activate  # Для Windows: venv\Scripts\activate
    ```

3.  **Установите зависимости:**
    ```bash
    pip install aiogram aiosqlite python-dotenv
    ```

4.  **Настройте конфигурацию:**
    - Создайте файл `.env` в корне проекта и добавьте в него API-токен вашего бота:
      ```
      BOT_API_TOKEN=12345:ABC-DEF12345
      ```
    - Откройте файл `config.py` и в словаре `SPECIALIST_GROUPS` укажите ID ваших чатов. Чаты должны быть супергруппами с включенной функцией "Темы".

5.  **Запустите бота:**
    ```bash
    python BOTDDMIET.py
    ```
